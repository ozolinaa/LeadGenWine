#!/bin/bash

# set permissions  | chmod +x set_aws_credentials.sh |  chmod +x /opt/scripts/set_aws_credentials.sh
# run once         | ./set_aws_credentials.sh

echo "Setting AWS credentials"
export AWSkey='AWS_KEY_VALUE'
export AWSsecret='AWS_SECRET_VALUE'
